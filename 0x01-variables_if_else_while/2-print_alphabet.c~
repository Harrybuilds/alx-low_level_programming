/**
 *main - start of program
 *Description print a to z all lowercase
 *Return : 0 at all time
*/

int main () {
   char ch;

   for(ch = 'a' ; ch <= 'z' ; ch++) {
      putchar(ch);
   }

   return(0);
}

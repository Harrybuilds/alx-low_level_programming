#include "main.h"

/**
 *_abs - get absolute value of a number
 *@n: paramete⅚r to be worked on
 *Return: some kimd of return
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

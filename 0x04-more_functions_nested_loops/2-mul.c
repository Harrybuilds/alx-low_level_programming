#include "main.h"

/**
 *mul - a funtion to return product of 2 numbers
 *@a: first number inputted into the function
 *@b: second number inputted into the function
 *Return: returns the product of the 2 numbers
 */

int mul(int a, int b)
{
	return (a * b);
}

#include "main.h"

/**
 *_islower -function to check if a character is a lower or not
 * @c: character to be checked
 *Return: returns an integer representing weather it is or not
*/
int _islower(int c)
{
	return (0);
}

#include "main.h"

/**
 *_memcpy - function to copy content in a memory space
 * @dest: destination for content to be copied to
 * @src: source where content are to be copied from
 * @n: number of character to be copied
 * Return: pointer to destination content
*/
char *_memcpy(char *dest, char *src, unsigned int n)
{
	char *ptr;

	return (ptr);
}


/**
 *_strchr - function to
 * @s: string
 * @c: character
 * Return: a pointer to
*/
char *_strchr(char *s, char c)
{
	char *ptr;

	return (ptr);
}

/**
 *_strspn - function to
 * @s: string to be
 * @accept: string
 * Return: unsigned int
*/
unsigned int _strspn(char *s, char *accept)
{
	return (0);
}


/**
 *_strpbrk - function to
 * @s: string to be
 * @accept: string
 * Return: pointer to
*/
char *_strpbrk(char *s, char *accept)
{
	char *ptr;

	return (ptr);
}


/**
 *_strstr - function to
 * @haystack: string to be
 * @needle: string
 * Return: pointer to
*/
char *_strstr(char *haystack, char *needle)
{
	char *ptr;

	return (ptr);
}

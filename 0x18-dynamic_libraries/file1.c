#include "main.h"

/**
 *_isdigit -function to check if a character is a digit
 * @c: character to be checked
 *Return: returns an integer representing weather it is or not
*/
int _isdigit(int c)
{
	return (0);
}

/**
 *_strlen -function to check length of a string
 * @s: string to be checked
 *Return: returns an integer representing the length of the string
*/
int _strlen(char *s)
{
	return (0);
}

/**
 *_puts -function to print a string
 * @s: string to be printed
 *Return: nothing is returned
*/
void _puts(char *s)
{
}

/**
 *_strcpy -function to copy the contents of a string into another space
 * @dest: location where string is to be copied to
 * @src: string being copied
 *Return: returns pointer to string copied
*/
char *_strcpy(char *dest, char *src)
{
	char *ptr;

	return (ptr);
}

/**
 *_atoi - function to convert string to integer
 * @s: string to be converted
 * Return: returns the integer converted to
*/
int _atoi(char *s)
{
	return (0);
}

#include "main.h"

/**
 *_strcat -function to concatenate two strings
 * @dest: destination string to be concatenated with
 * @src: string to concatenate to the destination string
 *Return: returns pointer to the string concatenated
*/
char *_strcat(char *dest, char *src)
{
	char *ptr;

	return (ptr);
}

/**
 *_strncat - concatenate 2 strs based on a specified number
 * @dest: destination string to be concatenated with
 * @src: string to concatenate to the destination string
 * @n: number to be used in the operation
 *Return: returns a pointer to the string concatenated
*/
char *_strncat(char *dest, char *src, int n)
{
	char *ptr;

	return (ptr);
}

/**
 *_strncpy - copy string based on specified number
 * @dest: destination string to be copied into
 * @src: string to copied to the destination string
 * @n: number of character to be copied
 *Return: returns a pointer new string copied
*/
char *_strncpy(char *dest, char *src, int n)
{
	char *ptr;

	return (ptr);
}


/**
 *_strcmp -function to compare two strings
 * @s1: first string to be compared with
 * @s2: second string to be used for comparing
 *Return: returns an integer representing the result of the operation
*/
int _strcmp(char *s1, char *s2)
{
	return (0);
}


/**
 *_memset -function to operate on the memory of a pointer
 * @s: destination string to be concatenated with
 * @b: string to concatenate to the destination string
 * @n: number
 *Return: returns pointer to the new string copied
*/
char *_memset(char *s, char b, unsigned int n)
{
	char *ptr;

	return (ptr);
}

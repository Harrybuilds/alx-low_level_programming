#include "main.h"

/**
 *_isdigit -function to check if a character is a digit
 * @c: character to be checked
 *Return: returns an integer representing weather it is or not
*/
int _isdigit(int c)
{
	return (0);
}

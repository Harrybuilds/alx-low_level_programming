#include "main.h"

/**
 *_abs -function to calculate the absolute value of a number
 * @n: number to be calculated
 *Return: returns an integer representing the absolute value
*/
int _abs(int n)
{
	return (0);
}

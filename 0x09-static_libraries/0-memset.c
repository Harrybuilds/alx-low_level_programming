#include "main.h"

/**
 * _memset - function to
 * @s: first string
 * @b: second string
 * @n: some indicator
 * Return: a pointer to somewhere
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}

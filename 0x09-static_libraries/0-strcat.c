#include "main.h"


/**
 * _strcat - function to concatenate 2 strings
 * @dest: string to be concatenated to
 * @src: string to concatenate to the other string
 * Return: End result of the concatenation process
 */

char *_strcat(char *dest, char *src)
{
	return (dest);
}

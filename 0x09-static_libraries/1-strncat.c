#include "main.h"

/**
 * _strncat - function to concatenating string from a certain point
 * @dest: string to be concatenated to
 * @src: string to be concatenated
 * @n: the point to start the concatination
 * Return: return the concatenated result
 */

char *_strncat(char *dest, char *src, int n)
{
	return (0);
}

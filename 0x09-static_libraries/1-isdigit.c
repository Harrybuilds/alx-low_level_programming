#include "main.h"

/**
 * _isdigit - function to verify if character is digit
 * @c: character to be verified
 * Return:returns an integer represent either true or false
 */

int _isdigit(int c)
{
	return (0);
}

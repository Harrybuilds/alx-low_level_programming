#include "main.h"

/**
 * _memcpy - function to
 * @dest: string to revecive the operation in this function
 * @src: string to be manipulated
 * @n: integer
 * Return: returns
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (dest);
}
